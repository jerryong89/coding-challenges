Two Makes 10
Create a function that has two integer parameters. The function should return true if either one of them is 10 or if their sum is 10.

Examples
makesTen(9, 10) → true

makesTen(9, 9) → false

makesTen(1, 9) → true

https://edabit.com/challenge/5erCDJ8eJDrXkmwTK

My Solution

function makesTen(a, b) {
	if (a === 10 || b === 10 || a+b === 10) {
		return true
	} else return false
}
